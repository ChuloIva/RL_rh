/.env
/.venv
/.claude/
deep/.DS_Store
.DS_Store

# Python caches
__pycache__/
*.pyc

# Video pipeline generated outputs (images, video, audio) — kept local only
deep/video/out/
deep/video/samples/

# Steering vectors / model-prep — a separate project, kept local only
/steering_lab/

